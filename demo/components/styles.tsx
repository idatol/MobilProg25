const cardWidth = "70%";

const ProductCardStyles = {
    width: cardWidth,
    backgroundColor: "White",
    borderRadius: 10,
    overflow: "hidden",
    shadowColor: "#000",
    shadowOffset: { width: 0, height: 4 },
    shadowOpacity: 0.2,
    shadowRadius: 5,
    elevation: 5
} as const;
